import { ApiResponse } from '../utils/ApiResponse.js';
import { ApiError } from '../utils/ApiError.js';
import { asyncHandler } from '../utils/AsyncHandler.js';

const getUsers = asyncHandler((req, res) => {
    
});

const createUser = asyncHandler((req, res) => {
    
});

const checkUserExist = asyncHandler((req, res) => {
    
});

const getRandomUser = asyncHandler((req, res) => {
    
});

const getUserAboveAge = asyncHandler((req, res) => {
    
});

export { getUserAboveAge, getUsers, getRandomUser, checkUserExist, createUser };